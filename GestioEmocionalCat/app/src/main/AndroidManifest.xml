<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestioemocionalcat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/salut_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AvaluacioActivityFinal" android:label="@string/app_avaluació"
            android:screenOrientation = "portrait"/>
        <activity android:name=".AvaluacioActivity3" android:label="@string/app_avaluació"
            android:screenOrientation = "portrait"/>
        <activity android:name=".AvaluacioActivity2" android:label="@string/app_avaluació"
            android:screenOrientation = "portrait"/>
        <activity android:name=".AvaluacioActivity" android:label="@string/app_avaluació"
            android:screenOrientation = "portrait"/>
        <activity android:name=".RecursosActivity" android:label="@string/app_recursos"
            android:screenOrientation = "portrait"/>
        <activity android:name=".MainActivity" android:screenOrientation = "portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>